---
'@atcute/lexicon-doc': minor
---

add an optional `subprotocol` field to subscription definitions, declaring a stream's default wire
subprotocol (such as `xrpc.v1.json`) for clients that don't negotiate one
