---
'@atcute/lex-cli': minor
'@atcute/lexicons': minor
---

carry a subscription's declared `subprotocol` on its generated metadata and
`XRPCSubscriptionMetadata`, exposing a stream's default wire encoding to servers and clients
