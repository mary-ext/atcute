---
'@atcute/bluesky-moderation': minor
---

add the `ProfileBio` display context for a profile's display name and description, blurred by forced
labels (`!hide`, `!warn`, `!no-unauthenticated`) but not by media labels such as `porn`
