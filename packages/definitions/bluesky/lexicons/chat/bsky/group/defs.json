{
	"id": "chat.bsky.group.defs",
	"defs": {
		"joinRule": {
			"type": "string",
			"knownValues": ["anyone", "followedByOwner"]
		},
		"joinLinkView": {
			"type": "object",
			"required": ["code", "createdAt", "enabledStatus", "joinRule", "requireApproval"],
			"properties": {
				"code": {
					"type": "string"
				},
				"joinRule": {
					"ref": "#joinRule",
					"type": "ref"
				},
				"createdAt": {
					"type": "string",
					"format": "datetime"
				},
				"enabledStatus": {
					"ref": "#linkEnabledStatus",
					"type": "ref"
				},
				"requireApproval": {
					"type": "boolean"
				}
			}
		},
		"joinRequestView": {
			"type": "object",
			"required": ["convoId", "requestedAt", "requestedBy"],
			"properties": {
				"convoId": {
					"type": "string"
				},
				"requestedAt": {
					"type": "string",
					"format": "datetime"
				},
				"requestedBy": {
					"ref": "chat.bsky.actor.defs#profileViewBasic",
					"type": "ref"
				}
			}
		},
		"linkEnabledStatus": {
			"type": "string",
			"knownValues": ["disabled", "enabled"]
		},
		"joinLinkPreviewView": {
			"type": "object",
			"required": ["memberCount", "name", "owner", "requireApproval"],
			"properties": {
				"name": {
					"type": "string"
				},
				"convo": {
					"ref": "chat.bsky.convo.defs#convoView",
					"type": "ref",
					"description": "Present only if the request is authenticated and the user is a member of the group."
				},
				"owner": {
					"ref": "chat.bsky.actor.defs#profileViewBasic",
					"type": "ref"
				},
				"memberCount": {
					"type": "integer"
				},
				"requireApproval": {
					"type": "boolean"
				}
			}
		}
	},
	"lexicon": 1,
	"description": "[NOTE: This is under active development and should be considered unstable while this note is here]."
}
