{
	"id": "app.bsky.video.getUploadStatus",
	"defs": {
		"main": {
			"type": "query",
			"errors": [
				{
					"name": "UploadNotFound",
					"description": "The job ID is unknown or aged out of retention; known terminal sessions remain readable and are never reported as not found."
				}
			],
			"output": {
				"schema": {
					"type": "object",
					"required": ["expiresAt", "jobId", "partCount", "partSizeBytes", "receivedParts", "state"],
					"properties": {
						"jobId": {
							"type": "string",
							"maxLength": 256,
							"minLength": 1
						},
						"state": {
							"type": "string",
							"maxLength": 32,
							"knownValues": ["aborted", "completed", "created", "expired", "failed", "finishing"]
						},
						"expiresAt": {
							"type": "string",
							"format": "datetime"
						},
						"jobStatus": {
							"ref": "app.bsky.video.defs#jobStatus",
							"type": "ref",
							"description": "Present only when state is completed."
						},
						"partCount": {
							"type": "integer"
						},
						"failureReason": {
							"type": "string",
							"maxLength": 1024,
							"description": "Present only when state is failed."
						},
						"partSizeBytes": {
							"type": "integer"
						},
						"receivedParts": {
							"type": "array",
							"items": {
								"type": "integer",
								"minimum": 1
							}
						},
						"completedJobId": {
							"type": "string",
							"maxLength": 256,
							"minLength": 1,
							"description": "Present only when state is completed; may differ from jobId on deduplication."
						}
					}
				},
				"encoding": "application/json"
			},
			"parameters": {
				"type": "params",
				"required": ["jobId"],
				"properties": {
					"jobId": {
						"type": "string",
						"maxLength": 256,
						"minLength": 1
					}
				}
			},
			"description": "Get the authoritative status of the upload phase. Terminal states remain readable. completedJobId and jobStatus are present only for completed sessions; failureReason is present only for failed sessions."
		}
	},
	"lexicon": 1
}
