{
	"id": "pub.leaflet.blocks.postHeader",
	"defs": {
		"main": {
			"type": "object",
			"required": [],
			"properties": {
				"compact": {
					"type": "boolean",
					"description": "Show a condensed header: title and byline only, without the description."
				}
			},
			"description": "The post's header (publication, title, description, byline) placed as a block, so canvas posts can position it. Renders the document's own metadata; carries no content of its own."
		}
	},
	"lexicon": 1
}
