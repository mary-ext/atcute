{
	"id": "sh.tangled.feed.countStars",
	"defs": {
		"main": {
			"type": "query",
			"output": {
				"schema": {
					"type": "object",
					"required": ["count", "distinctAuthors"],
					"properties": {
						"count": {
							"type": "integer",
							"minimum": 0,
							"description": "Total number of matching records."
						},
						"distinctAuthors": {
							"type": "integer",
							"minimum": 0,
							"description": "Number of distinct authors among the matching records."
						}
					}
				},
				"encoding": "application/json"
			},
			"parameters": {
				"type": "params",
				"required": ["subject"],
				"properties": {
					"subject": {
						"type": "string",
						"description": "Repo DID to list star edges for."
					}
				}
			}
		}
	},
	"lexicon": 1
}
