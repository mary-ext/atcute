{
	"lexicon": 1,
	"id": "sh.tangled.git.temp.listBranches",
	"defs": {
		"main": {
			"type": "query",
			"parameters": {
				"type": "params",
				"required": ["repo"],
				"properties": {
					"repo": {
						"type": "string",
						"format": "at-uri",
						"description": "AT-URI of the repository"
					},
					"limit": {
						"type": "integer",
						"description": "Maximum number of branches to return",
						"minimum": 1,
						"maximum": 100,
						"default": 50
					},
					"cursor": {
						"type": "string",
						"description": "Pagination cursor"
					}
				}
			},
			"output": {
				"encoding": "*/*"
			},
			"errors": [
				{
					"name": "RepoNotFound",
					"description": "Repository not found or access denied"
				},
				{
					"name": "InvalidRequest",
					"description": "Invalid request parameters"
				}
			]
		}
	}
}
