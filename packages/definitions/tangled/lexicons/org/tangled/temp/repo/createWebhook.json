{
	"id": "org.tangled.temp.repo.createWebhook",
	"defs": {
		"main": {
			"type": "procedure",
			"input": {
				"schema": {
					"type": "object",
					"required": ["events", "repoDid", "url"],
					"properties": {
						"url": {
							"type": "string",
							"format": "uri",
							"description": "Endpoint URL that will receive webhook payloads."
						},
						"active": {
							"type": "boolean",
							"description": "Whether the webhook should be active immediately. Defaults to true."
						},
						"events": {
							"type": "array",
							"items": {
								"type": "string"
							},
							"description": "Event types to subscribe to (e.g. 'push', 'repository:renamed')."
						},
						"secret": {
							"type": "string",
							"description": "Optional HMAC secret used to sign payloads. If omitted, payloads are not signed."
						},
						"repoDid": {
							"type": "string",
							"format": "did",
							"description": "DID of the repository as minted by the knot."
						}
					}
				},
				"encoding": "application/json"
			},
			"errors": [
				{
					"name": "NoEventsSelected",
					"description": "At least one event type must be specified."
				}
			],
			"output": {
				"schema": {
					"type": "object",
					"required": ["id"],
					"properties": {
						"id": {
							"type": "integer",
							"description": "ID of the newly created webhook."
						}
					}
				},
				"encoding": "application/json"
			},
			"description": "Create a new webhook for a repository."
		}
	},
	"lexicon": 1
}
